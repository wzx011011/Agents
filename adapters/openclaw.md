# OpenClaw / ZCode 适配指南

## 环境特征
- ZCode 底层调用 Code CLI（agent_id: "caude-code"）
- 使用 SQLite 管理工作区和会话
- 有 GUI 界面，支持多标签页
- 支持 MCP 服务器配置

## 文件放置
- 直接打开架构仓库目录作为工作区
- 或将架构文件复制到 ZCode workspace 目录

## 原生能力映射
与 Code CLI 相同，通过 GUI 界面操作。