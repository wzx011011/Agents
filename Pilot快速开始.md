# Pilot 快速开始

这个工作区现在已经包含一套最小可用的多 Agent 交付配置。

## 已包含角色

- `组合PM`
- `通用开发`
- `评审调试`
- `测试工程师`

## 已包含内容

- `.github/` 下的工作区 instructions
- `共享/模板/` 下的共享模板
- `组合/` 下的组合级状态文件
- `项目/` 下的 5 个项目记忆目录

## Pilot 工作流程

1. 选一个项目，通常建议 `项目-A`。
2. 用真实上下文填充 `项目简报.md`。
3. 在 `项目状态.yaml` 和 `待办列表.yaml` 中写入 1 个 ready 任务。
4. 使用 `组合PM` 细化第一份任务包。
5. 切换到 `通用开发` 执行开发或环境配置。
6. 切换到 `评审调试` 做评审或根因分析。
7. 切换到 `测试工程师` 做验证并更新测试报告。
8. 回到 `组合PM` 刷新项目状态并选择下一步。

## 最少要持续更新的文件

- `项目状态.yaml`
- `当前上下文.md`
- `交接.md`
- `验证/最新测试报告.md`

## 推荐第一步

先把 `项目-A` 当作 pilot，其余 4 个项目先保持占位，等流程稳定后再逐步激活。
